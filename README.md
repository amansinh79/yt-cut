`yc <youtube_url> -s <start_time> -e <end_time> -o <filename.mp4>`

time is required to be entered in hh:mm:ss format.

start and end times are optional. defaults to full video length.

you can specify -r. to re-encode instead of copying.

output is optional. defaults to output.mp4

depends on ffmpeg.
